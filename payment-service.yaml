server:
  port: 8086
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
stripe:
  secret: ${STRIPE_SECRET}
  webhookSecret: ${STRIPE_WEBHOOK_SECRET}
webUrl: ${WEB_URL:http://localhost:4200}

