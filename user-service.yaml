server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: "prometheus"
jwt:
  secret: ${JWT_SECRET}
  expiration: 3600

springdoc:
  api-docs:
    path: /users/v3/api-docs
